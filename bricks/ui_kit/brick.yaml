name: ui_kit
description: A brick that generates a Flutter package for UI components.

version: 0.3.0+5

environment:
  mason: ^0.1.0

vars:
  class_name:
    type: string
    description: The name of the class to generate
    default: UiKit
    prompt: Enter the name of the class to generate

import 'package:flutter/material.dart';

class UIKitPadding extends EdgeInsetsDirectional {
  /// Zero
  const UIKitPadding() : super.all(0);
  const UIKitPadding.zero() : super.all(0);

  /// Padding all
  /// Padding all 4
  const UIKitPadding.all4() : super.all(4);

  /// Padding all 8
  const UIKitPadding.all8() : super.all(8);

  /// Padding all 12
  const UIKitPadding.all12() : super.all(12);

  /// Padding all 16
  const UIKitPadding.all16() : super.all(16);

  /// Padding all 20
  const UIKitPadding.all20() : super.all(20);

  /// Padding all 24
  const UIKitPadding.all24() : super.all(24);

  /// Padding all 28
  const UIKitPadding.all28() : super.all(28);

  /// Padding all 32
  const UIKitPadding.all32() : super.all(32);

  /// Padding all 36
  const UIKitPadding.all36() : super.all(36);

  /// Padding all 40
  const UIKitPadding.all40() : super.all(40);

  /// Padding Horizonal Symmetric
  /// Padding Horizonal Symmetric 4
  const UIKitPadding.horizontalSymmetric4() : super.symmetric(horizontal: 4);

  /// Padding Horizonal Symmetric 8
  const UIKitPadding.horizontalSymmetric8() : super.symmetric(horizontal: 8);

  /// Padding Horizonal Symmetric 12
  const UIKitPadding.horizontalSymmetric12() : super.symmetric(horizontal: 12);

  /// Padding Horizonal Symmetric 16
  const UIKitPadding.horizontalSymmetric16() : super.symmetric(horizontal: 16);

  /// Padding Horizonal Symmetric 20
  const UIKitPadding.horizontalSymmetric20() : super.symmetric(horizontal: 20);

  /// Padding Horizonal Symmetric 24
  const UIKitPadding.horizontalSymmetric24() : super.symmetric(horizontal: 24);

  /// Padding Horizonal Symmetric 28
  const UIKitPadding.horizontalSymmetric28() : super.symmetric(horizontal: 28);

  /// Padding Horizonal Symmetric 32
  const UIKitPadding.horizontalSymmetric32() : super.symmetric(horizontal: 32);

  /// Padding Horizonal Symmetric 36
  const UIKitPadding.horizontalSymmetric36() : super.symmetric(horizontal: 36);

  /// Padding Horizonal Symmetric 40
  const UIKitPadding.horizontalSymmetric40() : super.symmetric(horizontal: 40);

  /// Padding Vertical Symmetric
  /// Padding Vertical Symmetric 4
  const UIKitPadding.verticalSymmetric4() : super.symmetric(vertical: 4);

  /// Padding Vertical Symmetric 8
  const UIKitPadding.verticalSymmetric8() : super.symmetric(vertical: 8);

  /// Padding Vertical Symmetric 12
  const UIKitPadding.verticalSymmetric12() : super.symmetric(vertical: 12);

  /// Padding Vertical Symmetric 16
  const UIKitPadding.verticalSymmetric16() : super.symmetric(vertical: 16);

  /// Padding Vertical Symmetric 20
  const UIKitPadding.verticalSymmetric20() : super.symmetric(vertical: 20);

  /// Padding Vertical Symmetric 24
  const UIKitPadding.verticalSymmetric24() : super.symmetric(vertical: 24);

  /// Padding Vertical Symmetric 28
  const UIKitPadding.verticalSymmetric28() : super.symmetric(vertical: 28);

  /// Padding Vertical Symmetric 32
  const UIKitPadding.verticalSymmetric32() : super.symmetric(vertical: 32);

  /// Padding Vertical Symmetric 36
  const UIKitPadding.verticalSymmetric36() : super.symmetric(vertical: 36);

  /// Padding Vertical Symmetric 40
  const UIKitPadding.verticalSymmetric40() : super.symmetric(vertical: 40);

  /// Padding Only Start
  /// Padding Only Start 4
  const UIKitPadding.onlyStart4() : super.only(start: 4);

  /// Padding Only Start 8
  const UIKitPadding.onlyStart8() : super.only(start: 8);

  /// Padding Only Start 12
  const UIKitPadding.onlyStart12() : super.only(start: 12);

  /// Padding Only Start 16
  const UIKitPadding.onlyStart16() : super.only(start: 16);

  /// Padding Only Start 20
  const UIKitPadding.onlyStart20() : super.only(start: 20);

  /// Padding Only Start 24
  const UIKitPadding.onlyStart24() : super.only(start: 24);

  /// Padding Only Start 28
  const UIKitPadding.onlyStart28() : super.only(start: 28);

  /// Padding Only Start 32
  const UIKitPadding.onlyStart32() : super.only(start: 32);

  /// Padding Only Start 36
  const UIKitPadding.onlyStart36() : super.only(start: 36);

  /// Padding Only Start 40
  const UIKitPadding.onlyStart40() : super.only(start: 40);

  /// Padding Only End
  /// Padding Only End 4
  const UIKitPadding.onlyEnd4() : super.only(end: 4);

  /// Padding Only End 8
  const UIKitPadding.onlyEnd8() : super.only(end: 8);

  /// Padding Only End 12
  const UIKitPadding.onlyEnd12() : super.only(end: 12);

  /// Padding Only End 16
  const UIKitPadding.onlyEnd16() : super.only(end: 16);

  /// Padding Only End 20
  const UIKitPadding.onlyEnd20() : super.only(end: 20);

  /// Padding Only End 24
  const UIKitPadding.onlyEnd24() : super.only(end: 24);

  /// Padding Only End 28
  const UIKitPadding.onlyEnd28() : super.only(end: 28);

  /// Padding Only End 32
  const UIKitPadding.onlyEnd32() : super.only(end: 32);

  /// Padding Only End 36
  const UIKitPadding.onlyEnd36() : super.only(end: 36);

  /// Padding Only End 40
  const UIKitPadding.onlyEnd40() : super.only(end: 40);

  /// Padding Only Top
  /// Padding Only Top 4
  const UIKitPadding.onlyTop4() : super.only(top: 4);

  /// Padding Only Top 8
  const UIKitPadding.onlyTop8() : super.only(top: 8);

  /// Padding Only Top 12
  const UIKitPadding.onlyTop12() : super.only(top: 12);

  /// Padding Only Top 16
  const UIKitPadding.onlyTop16() : super.only(top: 16);

  /// Padding Only Top 20
  const UIKitPadding.onlyTop20() : super.only(top: 20);

  /// Padding Only Top 24
  const UIKitPadding.onlyTop24() : super.only(top: 24);

  /// Padding Only Top 28
  const UIKitPadding.onlyTop28() : super.only(top: 28);

  /// Padding Only Top 32
  const UIKitPadding.onlyTop32() : super.only(top: 32);

  /// Padding Only Top 36
  const UIKitPadding.onlyTop36() : super.only(top: 36);

  /// Padding Only Top 40
  const UIKitPadding.onlyTop40() : super.only(top: 40);

  /// Padding Only Bottom
  /// Padding Only Bottom 4
  const UIKitPadding.onlyBottom4() : super.only(bottom: 4);

  /// Padding Only Bottom 8
  const UIKitPadding.onlyBottom8() : super.only(bottom: 8);

  /// Padding Only Bottom 12
  const UIKitPadding.onlyBottom12() : super.only(bottom: 12);

  /// Padding Only Bottom 16
  const UIKitPadding.onlyBottom16() : super.only(bottom: 16);

  /// Padding Only Bottom 20
  const UIKitPadding.onlyBottom20() : super.only(bottom: 20);

  /// Padding Only Bottom 24
  const UIKitPadding.onlyBottom24() : super.only(bottom: 24);

  /// Padding Only Bottom 28
  const UIKitPadding.onlyBottom28() : super.only(bottom: 28);

  /// Padding Only Bottom 32
  const UIKitPadding.onlyBottom32() : super.only(bottom: 32);

  /// Padding Only Bottom 36
  const UIKitPadding.onlyBottom36() : super.only(bottom: 36);

  /// Padding Only Bottom 40
  const UIKitPadding.onlyBottom40() : super.only(bottom: 40);
}
